@if(countryDTO is null)
{
    <Loading/>
}
else
{
    <MudDialog>
        <DialogContent>
            <CountryForm @ref="countryForm" CountryDTO="countryDTO" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}