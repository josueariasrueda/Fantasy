@if (tenant is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <TenantForm @ref="tenantForm" Tenant="tenant" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}